---
// src/pages/index.astro
---

<html lang="es">
	<head>
        <script is:inline>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
        new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
        j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
        'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
        })(window,document,'script','dataLayer','GTM-52KCG6F3');</script>
        
        <meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.png" />
		<meta name="viewport" content="width=device-width" />
        <meta name="description" content="Encontramos y negociamos tu próximo auto por vos. Personal Shopper Automotriz en Paraguay." />
		<title>Personal Shopper Automotriz | Datacar</title>
        
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&family=Montserrat:wght@300;900&display=swap" rel="stylesheet">
        
        <style>
            h1, h2, h3, h4 { font-family: 'Montserrat', sans-serif; }
            body, p, a, input, textarea, select { font-family: 'Inter', sans-serif; }
            html { scroll-behavior: smooth; }
            .data-row { border-bottom: 1px solid #f3f4f6; padding: 12px 0; display: flex; flex-direction: column; }
            .data-label { font-size: 0.65rem; text-transform: uppercase; letter-spacing: 0.1em; color: #9ca3af; font-weight: 800; margin-bottom: 2px; }
            .data-value { font-size: 0.85rem; font-weight: 600; line-height: 1.3; color: #1f2937; }
            optgroup { font-weight: 800; color: #0A1F33; background-color: #f3f4f6; }
        </style>
        <script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body class="bg-gray-50 text-[#3A3A3C] antialiased">
		<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-52KCG6F3"
        height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
        
        <nav class="absolute top-0 w-full z-50 py-6 px-4">
			<div class="max-w-7xl mx-auto flex justify-between items-center">
                <a href="/" class="flex items-center group">
                    <span class="text-2xl md:text-3xl font-[900] text-white leading-none">DATA</span>
                    <span class="text-2xl md:text-3xl font-[300] text-[#C0C0C0] leading-none tracking-[1px]">CAR</span>
                </a>
				<div class="hidden md:flex space-x-8 items-center text-white">
					<a href="/servicios" class="hover:text-[#00BFFF] transition text-sm font-medium uppercase tracking-widest">Servicios</a>
                    <a href="/catalogo" class="hover:text-[#00BFFF] transition text-sm font-medium uppercase tracking-widest">Catálogo</a>
                    <a href="#comparador" class="hover:text-[#00BFFF] transition text-sm font-medium uppercase tracking-widest">Comparador</a>
                    <a href="#contacto" class="border-2 border-[#00BFFF] text-[#00BFFF] px-6 py-2 rounded-full text-xs font-bold hover:bg-[#00BFFF] hover:text-[#0A1F33] transition tracking-widest uppercase">Activar Búsqueda</a>
				</div>
			</div>
		</nav>

        <section class="relative bg-[#0A1F33] text-white min-h-screen flex items-center">
            <div class="absolute inset-0 z-0">
                <img src="/hero-image.webp" class="w-full h-full object-cover opacity-30" onerror="this.src='/hero-image.jpg'" />
                <div class="absolute inset-0 bg-gradient-to-b from-[#0A1F33]/90 to-[#0A1F33]"></div>
            </div>
            <div class="relative z-10 max-w-5xl mx-auto px-4 text-center mt-16">
                <span class="text-[#00BFFF] font-bold tracking-[0.3em] uppercase text-[10px] mb-6 inline-block border border-[#00BFFF]/30 px-4 py-1 rounded-full bg-[#00BFFF]/10">Personal Shopper Automotriz</span>
                <h1 class="text-4xl md:text-7xl font-[900] mb-6 leading-tight tracking-tighter">
                    Encontramos y Negociamos tu Próximo Auto por Vos.
                </h1>
                <p class="text-lg md:text-xl text-gray-300 max-w-3xl mb-10 mx-auto font-light leading-relaxed">
                    Ya sea un 0km o un usado seguro. Olvidate de recorrer concesionarias o lidiar con vendedores. <span class="text-white font-bold">Nosotros hacemos el trabajo sucio.</span>
                </p>
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a href="#contacto" class="bg-[#00BFFF] text-[#0A1F33] font-[900] py-5 px-12 rounded-xl text-lg uppercase shadow-xl transition hover:scale-105">Empezar mi Búsqueda</a>
                    <a href="#comparador" class="border-2 border-white/30 text-white font-[700] py-5 px-12 rounded-xl text-lg backdrop-blur-sm hover:bg-white/10 transition">Ver Comparador</a>
                </div>
            </div>
        </section>

        <section class="py-24 bg-white overflow-hidden">
            <div class="max-w-7xl mx-auto px-4">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
                    <div>
                        <h2 class="text-3xl md:text-5xl font-[900] text-[#0A1F33] mb-8 leading-tight uppercase">¿Por qué nuestro servicio 0km es 100% gratis?</h2>
                        <p class="text-gray-600 text-lg mb-8 font-light leading-relaxed">
                            Gracias a nuestros acuerdos con las representantes oficiales en Paraguay. Ellas nos pagan por acercarles compradores decididos. Vos te llevás nuestra <span class="text-[#0A1F33] font-bold">experiencia negociadora</span> y el ahorro de tiempo, sin pagar un solo guaraní extra.
                        </p>
                        <div class="flex items-center gap-4 p-6 bg-gray-50 rounded-2xl border-l-4 border-[#00BFFF]">
                            <div class="text-3xl text-[#00BFFF]">“</div>
                            <p class="font-bold italic text-[#0A1F33]">Caso de éxito: Ahorramos US$ 1.500 en la compra de la camioneta de Jorge y gestionamos su transferencia en tiempo récord.</p>
                        </div>
                    </div>
                    <div class="relative">
                        <img src="/service-shopper.webp" class="relative rounded-[2.5rem] shadow-2xl" alt="Negociación Datacar" />
                    </div>
                </div>
            </div>
        </section>

        <section id="comparador" class="py-24 bg-[#F5F7FA] border-y border-gray-200">
            <div class="max-w-7xl mx-auto px-4">
                <div class="text-center mb-16">
                    <h2 class="text-3xl md:text-5xl font-[900] text-[#0A1F33] mb-4 uppercase tracking-tighter">Veredicto Técnico</h2>
                    <p class="text-gray-500 text-lg">Enfrentamos los datos crudos para que tu decisión sea 100% racional.</p>
                </div>

                <div id="loader" class="text-center py-10">
                    <div class="inline-block animate-spin rounded-full h-12 w-12 border-t-2 border-[#00BFFF]"></div>
                    <p class="mt-4 text-[#0A1F33] font-bold uppercase tracking-widest text-xs" id="loader-status">Sincronizando Mercado...</p>
                </div>

                <div id="comparador-ui" class="hidden">
                    <div class="bg-white rounded-[2rem] p-8 border border-gray-200 shadow-sm mb-12">
                        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 items-end">
                            <div>
                                <label class="block text-[10px] font-black text-gray-400 mb-2 uppercase tracking-[0.2em]">Formato</label>
                                <select id="filtroTipo" class="w-full bg-gray-50 border border-gray-200 text-[#0A1F33] text-sm rounded-xl p-4 font-bold outline-none">
                                    <option value="Todos">Todos los formatos</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-[10px] font-black text-gray-400 mb-2 uppercase tracking-[0.2em]">Motorización</label>
                                <select id="filtroCombustible" class="w-full bg-gray-50 border border-gray-200 text-[#0A1F33] text-sm rounded-xl p-4 font-bold outline-none">
                                    <option value="Todos">Cualquier Motor</option>
                                    <option value="Tradicional">Combustión</option>
                                    <option value="Hibridos">Híbridos</option>
                                    <option value="Electrico">Eléctricos</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-[10px] font-black text-gray-400 mb-2 uppercase tracking-[0.2em]">Presupuesto</label>
                                <select id="filtroPrecio" class="w-full bg-gray-50 border border-gray-200 text-[#0A1F33] text-sm rounded-xl p-4 font-bold outline-none">
                                    <option value="Todos">Sin límite</option>
                                    <option value="15000">Hasta US$ 15.000</option>
                                    <option value="25000">Hasta US$ 25.000</option>
                                    <option value="35000">Hasta US$ 35.000</option>
                                    <option value="50000">Hasta US$ 50.000</option>
                                    <option value="80000">Hasta US$ 80.000</option>
                                </select>
                            </div>
                            <button id="btnLimpiar" class="text-[#00BFFF] font-bold underline text-sm pb-4">Limpiar</button>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-10">
                        <div class="bg-white rounded-[2.5rem] shadow-xl border border-gray-100 flex flex-col overflow-hidden">
                            <div class="bg-gray-50 p-8 border-b">
                                <div class="space-y-4">
                                    <select id="marca1" class="w-full border p-4 rounded-xl font-bold bg-white outline-none"><option value="">Marca</option></select>
                                    <select id="modelo1" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Modelo</option></select>
                                    <select id="version1" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Versión</option></select>
                                </div>
                            </div>
                            <div id="resultado1" class="hidden p-8 flex-grow">
                                <img id="img1" src="" class="h-48 w-full object-contain mb-8 mix-blend-multiply" />
                                <h4 id="nombre1" class="text-2xl font-black text-[#0A1F33] mb-8 leading-tight"></h4>
                                <div class="data-row"><span class="data-label">Precio</span><span id="precio1" class="data-value text-[#00BFFF] font-black text-2xl"></span></div>
                                <div class="data-row"><span class="data-label">Mecánica</span><span id="mec1" class="data-value"></span></div>
                                <div class="data-row"><span class="data-label">Seguridad / ADAS</span><span id="seg1" class="data-value"></span></div>
                                <div class="data-row border-none"><span class="data-label">Garantía</span><span id="gar1" class="data-value text-xs text-gray-500"></span></div>
                                <a id="cta1" href="#" target="_blank" class="block w-full bg-[#0A1F33] text-white text-center py-5 rounded-2xl mt-10 font-black uppercase text-sm tracking-widest shadow-lg transition">Negociar este Auto</a>
                            </div>
                        </div>
                        <div class="bg-white rounded-[2.5rem] shadow-xl border border-gray-100 flex flex-col overflow-hidden">
                            <div class="bg-gray-50 p-8 border-b">
                                <div class="space-y-4">
                                    <select id="marca2" class="w-full border p-4 rounded-xl font-bold bg-white outline-none"><option value="">Marca</option></select>
                                    <select id="modelo2" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Modelo</option></select>
                                    <select id="version2" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Versión</option></select>
                                </div>
                            </div>
                            <div id="resultado2" class="hidden p-8 flex-grow">
                                <img id="img2" src="" class="h-48 w-full object-contain mb-8 mix-blend-multiply" />
                                <h4 id="nombre2" class="text-2xl font-black text-[#0A1F33] mb-8 leading-tight"></h4>
                                <div class="data-row"><span class="data-label">Precio</span><span id="precio2" class="data-value text-[#00BFFF] font-black text-2xl"></span></div>
                                <div class="data-row"><span class="data-label">Mecánica</span><span id="mec2" class="data-value"></span></div>
                                <div class="data-row"><span class="data-label">Seguridad / ADAS</span><span id="seg2" class="data-value"></span></div>
                                <div class="data-row border-none"><span class="data-label">Garantía</span><span id="gar2" class="data-value text-xs text-gray-500"></span></div>
                                <a id="cta2" href="#" target="_blank" class="block w-full bg-[#0A1F33] text-white text-center py-5 rounded-2xl mt-10 font-black uppercase text-sm tracking-widest shadow-lg transition">Negociar este Auto</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section id="contacto" class="py-24 bg-[#0A1F33]">
            <div class="max-w-xl mx-auto px-4">
                <div class="bg-white rounded-[3rem] p-10 md:p-14 shadow-2xl text-center">
                    <h3 class="text-2xl md:text-3xl font-black mb-2 text-[#0A1F33] uppercase">¿Lo buscamos por vos?</h3>
                    <p class="text-gray-400 mb-8 font-light">Dejanos tus datos y un Personal Shopper te contactará hoy.</p>
                    <form id="lead-form" class="space-y-5 text-left">
                        <input type="text" name="nombre" required placeholder="Nombre Completo" class="w-full border-b-2 border-gray-100 p-4 outline-none focus:border-[#00BFFF] transition text-lg">
                        <input type="tel" name="whatsapp" required placeholder="WhatsApp" class="w-full border-b-2 border-gray-100 p-4 outline-none focus:border-[#00BFFF] transition text-lg">
                        <select name="interes" class="w-full border-b-2 border-gray-100 p-4 outline-none text-gray-500 bg-transparent">
                            <option value="0km">Busco un 0km</option>
                            <option value="usado">Busco un Usado Seguro</option>
                            <option value="vender">Quiero vender mi auto</option>
                        </select>
                        <textarea name="mensaje" placeholder="¿Qué modelo o presupuesto tenés en mente?" class="w-full border-b-2 border-gray-100 p-4 outline-none focus:border-[#00BFFF] transition min-h-[100px]"></textarea>
                        <button type="submit" id="submit-btn" class="w-full bg-[#00BFFF] text-[#0A1F33] font-[900] py-6 rounded-2xl uppercase tracking-widest shadow-xl hover:scale-[1.02] transition-transform">Activar mi Búsqueda</button>
                    </form>
                </div>
            </div>
        </section>

        <footer class="bg-[#05111D] text-gray-600 py-12 text-center border-t border-white/5">
            <p class="text-[10px] uppercase tracking-[0.3em]">© 2026 DATACAR EAS. Gestión inteligente de inversiones automotrices.</p>
        </footer>

        <a href="https://wa.me/595991244469" target="_blank" class="fixed bottom-6 right-6 z-50 bg-[#25D366] text-white p-5 rounded-full shadow-2xl hover:scale-110 transition-transform"
           onclick="window.dataLayer.push({'event': 'whatsapp_click', 'location': 'sticky'});">
            <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51a12.8 12.8 0 0 0-.57-.01c-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 0 1-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 0 1-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 0 1 2.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0 0 12.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 0 0 5.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 0 0-3.48-8.413Z"/></svg>
        </a>

        <script type="module" is:inline>
            import Papa from 'https://cdn.jsdelivr.net/npm/papaparse@5.4.1/+esm';

            const URL_CSV = "https://docs.google.com/spreadsheets/d/e/2PACX-1vQgA9AGiaH-RdEQrO2mOSjNDe7_zPoBMZyp-Yzmc0IKkTo9CluAzhWRJ9JXT-4XgNAAgiF-Uzzsea5m/pub?output=csv";
            const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbyTBqtK9wDzl10vc226zFwn4f-v-DVjKCfo9JVj3XCcepIcw41NQHchLrbDK6mz1zWgWg/exec';

            let bdOriginal = [];
            let bdFiltrada = [];
            let colAA = 'Tipo Carrocería';
            let arbolFiltros = {};

            // Formulario
            document.getElementById('lead-form')?.addEventListener('submit', async (e) => {
                e.preventDefault();
                const btn = document.getElementById('submit-btn');
                btn.disabled = true; btn.innerText = "ENVIANDO...";
                try {
                    await fetch(SCRIPT_URL, { method: 'POST', body: new FormData(e.target) });
                    if(window.dataLayer) window.dataLayer.push({'event': 'lead_success'});
                    window.location.href = '/gracias';
                } catch {
                    alert("Error de conexión. Por favor intentá por WhatsApp.");
                    btn.disabled = false; btn.innerText = "ACTIVAR BÚSQUEDA";
                }
            });

            // Función para buscar columna de seguridad dinámicamente
            function findSafetyVal(car) {
                const keys = Object.keys(car);
                const safetyCol = keys.find(k => k.toLowerCase().includes('adas') || k.toLowerCase().includes('seguridad')) || "";
                return car[safetyCol]?.trim() || "";
            }

            async function init() {
                try {
                    const res = await fetch(URL_CSV, { cache: "no-store" });
                    const text = await res.text();
                    
                    Papa.parse(text, {
                        header: true, skipEmptyLines: true, transformHeader: h => h.trim(),
                        complete: (results) => {
                            try {
                                bdOriginal = results.data.filter(r => r.Marca && r.Marca.trim() !== "");
                                const headers = results.meta.fields;
                                if(headers.length > 26) colAA = headers[26];
                                
                                poblarCategorias();
                                asignarEventos();
                                aplicarFiltros();

                                document.getElementById('loader').classList.add('hidden');
                                document.getElementById('comparador-ui').classList.remove('hidden');
                            } catch (err) {
                                document.getElementById('loader-status').innerText = "Error procesando datos.";
                                console.error(err);
                            }
                        }
                    });
                } catch(e) { 
                    document.getElementById('loader-status').innerText = "Error de conexión.";
                }
            }

            function poblarCategorias() {
                const tipos = [...new Set(bdOriginal.map(c => c[colAA]?.trim()).filter(Boolean))].sort();
                const select = document.getElementById('filtroTipo');
                const grupos = { 'SUV': [], 'Pick-up': [], 'Sedán': [], 'Hatchback': [], 'Otros': [] };
                
                tipos.forEach(t => {
                    const tl = t.toLowerCase();
                    if(tl.includes('suv')) grupos.SUV.push(t);
                    else if(tl.includes('pick')) grupos['Pick-up'].push(t);
                    else if(tl.includes('sed')) grupos.Sedán.push(t);
                    else if(tl.includes('hatch')) grupos.Hatchback.push(t);
                    else grupos.Otros.push(t);
                });

                select.innerHTML = '<option value="Todos">Todos los formatos</option>';
                for(const [g, items] of Object.entries(grupos)) {
                    if(items.length) {
                        const og = document.createElement('optgroup'); og.label = g;
                        items.forEach(i => og.appendChild(new Option(i, i)));
                        select.appendChild(og);
                    }
                }
            }

            function asignarEventos() {
                ['filtroTipo', 'filtroCombustible', 'filtroPrecio'].forEach(id => {
                    document.getElementById(id).addEventListener('change', aplicarFiltros);
                });
                document.getElementById('btnLimpiar').onclick = () => {
                    document.getElementById('filtroTipo').value = 'Todos';
                    document.getElementById('filtroCombustible').value = 'Todos';
                    document.getElementById('filtroPrecio').value = 'Todos';
                    aplicarFiltros();
                };
                [1, 2].forEach(col => {
                    document.getElementById(`marca${col}`).onchange = () => updateModelos(col);
                    document.getElementById(`modelo${col}`).onchange = () => updateVersiones(col);
                    document.getElementById(`version${col}`).onchange = () => mostrarAuto(col);
                });
            }

            function aplicarFiltros() {
                const fT = document.getElementById('filtroTipo').value;
                const fC = document.getElementById('filtroCombustible').value;
                const fP = document.getElementById('filtroPrecio').value;

                bdFiltrada = bdOriginal.filter(c => {
                    const pT = fT === 'Todos' || c[colAA]?.trim() === fT;
                    const vP = parseInt(c['Precio (US$)']?.replace(/\D/g, '')) || 0;
                    const pP = fP === 'Todos' || vP <= parseInt(fP);
                    const vL = (c.Combustible || "").toLowerCase();
                    const cat = vL.includes('hibrid') || vL.includes('hev') ? 'Hibridos' : (vL.includes('elect') || vL.includes('ev') ? 'Electrico' : 'Tradicional');
                    const pC = fC === 'Todos' || cat === fC;
                    return pT && pP && pC;
                });

                arbolFiltros = {};
                bdFiltrada.forEach(c => {
                    const m = c.Marca.trim(), mo = (c.Modelo||"").trim(), v = (c.Versión||"Única").trim();
                    if(!arbolFiltros[m]) arbolFiltros[m] = {};
                    if(mo && !arbolFiltros[m][mo]) arbolFiltros[m][mo] = [];
                    if(mo && !arbolFiltros[m][mo].includes(v)) arbolFiltros[m][mo].push(v);
                });

                [1, 2].forEach(n => {
                    const s = document.getElementById(`marca${n}`);
                    s.innerHTML = '<option value="">Marca</option>';
                    Object.keys(arbolFiltros).sort().forEach(m => s.add(new Option(m, m)));
                    document.getElementById(`modelo${n}`).innerHTML = '<option value="">Modelo</option>';
                    document.getElementById(`modelo${n}`).disabled = true;
                    document.getElementById(`version${n}`).innerHTML = '<option value="">Versión</option>';
                    document.getElementById(`version${n}`).disabled = true;
                    document.getElementById(`resultado${n}`).classList.add('hidden');
                });
            }

            function updateModelos(n) {
                const m = document.getElementById(`marca${n}`).value;
                const s = document.getElementById(`modelo${n}`);
                s.innerHTML = '<option value="">Modelo</option>';
                if(m && arbolFiltros[m]) {
                    Object.keys(arbolFiltros[m]).sort().forEach(mo => s.add(new Option(mo, mo)));
                    s.disabled = false;
                } else s.disabled = true;
                document.getElementById(`version${n}`).disabled = true;
                document.getElementById(`resultado${n}`).classList.add('hidden');
            }

            function updateVersiones(n) {
                const m = document.getElementById(`marca${n}`).value;
                const mo = document.getElementById(`modelo${n}`).value;
                const s = document.getElementById(`version${n}`);
                s.innerHTML = '<option value="">Versión</option>';
                if(mo && arbolFiltros[m][mo]) {
                    arbolFiltros[m][mo].sort().forEach(v => s.add(new Option(v, v)));
                    s.disabled = false;
                } else s.disabled = true;
                document.getElementById(`resultado${n}`).classList.add('hidden');
            }

            function mostrarAuto(n) {
                const m = document.getElementById(`marca${n}`).value;
                const mo = document.getElementById(`modelo${n}`).value;
                const v = document.getElementById(`version${n}`).value;
                const car = bdFiltrada.find(c => c.Marca.trim() === m && (c.Modelo||"").trim() === mo && (c.Versión || "Única").trim() === v);
                
                if(car) {
                    document.getElementById(`nombre${n}`).innerText = `${m} ${mo} ${v !== 'Única' ? v : ''}`;
                    document.getElementById(`img${n}`).src = car['URL de Imagen'] || '';
                    document.getElementById(`precio${n}`).innerText = car['Precio (US$)'] || 'Consultar';
                    document.getElementById(`mec${n}`).innerText = `${car['Combustible'] || '-'} | ${car['Motor (Cilindrada / HP / Torque)'] || '-'}`;
                    
                    const sVal = findSafetyVal(car);
                    const elSeg = document.getElementById(`seg${n}`);
                    if(sVal) {
                        elSeg.innerText = sVal;
                        elSeg.style.color = "#1f2937";
                    } else {
                        elSeg.innerText = "Consultar ADAS con asesor";
                        elSeg.style.color = "#dc2626";
                    }

                    document.getElementById(`gar${n}`).innerText = car['Garantía'] || 'Consultar';
                    const msg = encodeURIComponent(`Hola Datacar, me interesa el ${m} ${mo} ${v}. Quiero asesoría.`);
                    document.getElementById(`cta${n}`).href = `https://wa.me/595991244469?text=${msg}`;
                    document.getElementById(`resultado${n}`).classList.remove('hidden');
                }
            }

            init();
        </script>
	</body>
</html>
