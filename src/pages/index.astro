---
// src/pages/index.astro
---

<html lang="es">
	<head>
        <script is:inline>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
        new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
        j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
        'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
        })(window,document,'script','dataLayer','GTM-52KCG6F3');</script>
        
        <meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.png" />
		<meta name="viewport" content="width=device-width" />
        
        <meta name="description" content="Especialistas en la compra y venta de autos 0km y usados en Paraguay. Negociamos por vos para conseguir el mejor precio con reportes técnicos de mercado." />
		<title>Compra y venta de autos 0km y usados en Paraguay | Datacar</title>
        
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Montserrat:wght@700;800;900&display=swap" rel="stylesheet">
        
        <script src="https://cdn.tailwindcss.com"></script>

        <style>
            h1, h2, h3, h4 { font-family: 'Montserrat', sans-serif; }
            body, p, a, input, textarea, select { font-family: 'Inter', sans-serif; }
            html { scroll-behavior: smooth; }
            
            /* Ficha Técnica Premium Legible (Sin Iconos) */
            .spec-card-row {
                display: flex;
                flex-direction: column;
                padding: 12px 16px;
                background: #ffffff;
                border-radius: 12px;
                border: 1px solid #e2e8f0;
                margin-bottom: 8px;
            }
            .spec-label {
                font-size: 0.65rem;
                font-weight: 800;
                color: #94a3b8;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                margin-bottom: 3px;
            }
            .spec-value {
                font-size: 0.9rem;
                font-weight: 600;
                color: #1e293b;
                line-height: 1.4;
            }
            .highlight-price-card {
                background: #f0f9ff;
                border: 2px solid #00BFFF;
                padding: 18px;
            }
            .highlight-price-card .spec-value {
                color: #00BFFF;
                font-size: 1.6rem;
                font-weight: 900;
            }
            optgroup { font-weight: 800; color: #0A1F33; background-color: #f3f4f6; }
        </style>
	</head>
	<body class="bg-slate-50 text-[#3A3A3C] antialiased">
		<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-52KCG6F3" height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
        
        <nav class="absolute top-0 w-full z-50 py-6 px-4">
			<div class="max-w-7xl mx-auto flex justify-between items-center">
                <a href="/" class="flex items-center group">
                    <span class="text-2xl md:text-3xl font-[900] text-white leading-none tracking-tighter uppercase">DATA<span class="font-[300] text-[#C0C0C0] tracking-[1px]">CAR</span></span>
                </a>
				<div class="hidden md:flex space-x-8 items-center text-white">
					<a href="/servicios" class="hover:text-[#00BFFF] transition text-xs font-bold uppercase tracking-widest">Servicios</a>
                    <a href="/catalogo" class="hover:text-[#00BFFF] transition text-xs font-bold uppercase tracking-widest">Catálogo</a>
                    <a href="#comparador" class="hover:text-[#00BFFF] transition text-xs font-bold uppercase tracking-widest">Comparador</a>
                    <a href="#contacto" class="border-2 border-[#00BFFF] text-[#00BFFF] px-6 py-2 rounded-full text-[10px] font-black hover:bg-[#00BFFF] hover:text-[#0A1F33] transition uppercase">Activar Búsqueda</a>
				</div>
			</div>
		</nav>

        <section class="relative bg-[#0A1F33] text-white min-h-screen flex items-center">
            <div class="absolute inset-0 z-0">
                <img src="/hero-image.webp" class="w-full h-full object-cover opacity-30" onerror="this.src='/hero-image.jpg'" />
                <div class="absolute inset-0 bg-gradient-to-b from-[#0A1F33]/90 to-[#0A1F33]"></div>
            </div>
            <div class="relative z-10 max-w-5xl mx-auto px-4 text-center mt-16">
                <span class="text-[#00BFFF] font-bold tracking-[0.4em] uppercase text-[10px] mb-6 inline-block border border-[#00BFFF]/30 px-4 py-1 rounded-full bg-[#00BFFF]/10">Negociación Automotriz</span>
                <h1 class="text-4xl md:text-7xl font-[900] mb-6 leading-tight tracking-tighter uppercase">
                    Compra y venta de autos 0km y usados en Paraguay.
                </h1>
                <p class="text-lg md:text-xl text-slate-300 max-w-3xl mb-10 mx-auto font-light leading-relaxed">
                    Elegí el auto, nosotros nos encargamos de que pagues el precio más bajo. Sin recorrer playas, sin lidiar con vendedores. <span class="text-white font-bold">Hacemos el trabajo sucio por vos.</span>
                </p>
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a href="#contacto" class="bg-[#00BFFF] text-[#0A1F33] font-[900] py-5 px-12 rounded-xl text-lg uppercase shadow-xl transition hover:scale-105">Activar mi Búsqueda</a>
                    <a href="#comparador" class="border-2 border-white/30 text-white font-[700] py-5 px-12 rounded-xl text-lg backdrop-blur-sm hover:bg-white/10 transition">Ver Comparador</a>
                </div>
            </div>
		</section>

        <section class="py-24 bg-white">
            <div class="max-w-7xl mx-auto px-4">
                <div class="text-center mb-20">
                    <h2 class="text-3xl md:text-5xl font-[900] text-[#0A1F33] mb-6 uppercase tracking-tight leading-none italic">El modelo tradicional <br/><span class="text-[#00BFFF] not-italic">está roto</span></h2>
                    <p class="text-slate-500 max-w-2xl mx-auto text-lg font-light">Visitar playas de autos y negociar con vendedores no es eficiente. Así es como Datacar cambia las reglas:</p>
                </div>
                
                <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                    <div class="p-10 rounded-[2.5rem] bg-slate-50 border border-slate-100 shadow-sm hover:shadow-md transition">
                        <h3 class="text-xl font-black text-[#0A1F33] mb-4 uppercase leading-tight">Basta de recorrer</h3>
                        <p class="text-slate-600 leading-relaxed font-light">¿Por qué perder tus sábados visitando concesionarias? Nosotros filtramos el mercado por vos, verificamos stock real y descartamos unidades problemáticas.</p>
                    </div>
                    <div class="p-10 rounded-[2.5rem] bg-slate-50 border border-slate-100 shadow-sm hover:shadow-md transition">
                        <h3 class="text-xl font-black text-[#0A1F33] mb-4 uppercase leading-tight">Sin negociar con 5 vendedores</h3>
                        <p class="text-slate-600 leading-relaxed font-light">No pelees el precio con extraños. Datacar utiliza su volumen de compra y red de contactos para presionar y bajar el precio final por vos.</p>
                    </div>
                    <div class="p-10 rounded-[2.5rem] bg-slate-50 border border-slate-100 shadow-sm hover:shadow-md transition">
                        <h3 class="text-xl font-black text-[#0A1F33] mb-4 uppercase leading-tight">Decidí con información</h3>
                        <p class="text-slate-600 leading-relaxed font-light">La mayoría compra por emoción. Nosotros te entregamos datos técnicos y comparativos reales para que tu inversión sea 100% racional y segura.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="py-24 bg-slate-900 text-white overflow-hidden">
            <div class="max-w-7xl mx-auto px-4">
                <div class="flex flex-col lg:flex-row items-center gap-20">
                    <div class="lg:w-1/2">
                        <h2 class="text-3xl md:text-5xl font-[900] text-[#00BFFF] mb-8 uppercase leading-tight">Nuestra Metodología: <br/>El Reporte Ejecutivo</h2>
                        <p class="text-xl text-slate-300 mb-8 font-light leading-relaxed">
                            No enviamos links sueltos. Te entregamos un documento profesional con las **5 mejores opciones del mercado** según tu presupuesto e intereses.
                        </p>
                        <div class="space-y-6 mb-10">
                            <div class="flex items-start gap-4">
                                <div class="bg-[#00BFFF] text-[#0A1F33] rounded-full w-7 h-7 flex items-center justify-center font-black shrink-0 mt-1">1</div>
                                <p class="text-slate-300"><span class="text-white font-bold">Análisis Técnico:</span> Filtramos mecánicamente cada unidad finalista.</p>
                            </div>
                            <div class="flex items-start gap-4">
                                <div class="bg-[#00BFFF] text-[#0A1F33] rounded-full w-7 h-7 flex items-center justify-center font-black shrink-0 mt-1">2</div>
                                <p class="text-slate-300"><span class="text-white font-bold">Auditoría de Seguridad:</span> Comparamos ADAS y equipamiento real.</p>
                            </div>
                            <div class="flex items-start gap-4">
                                <div class="bg-[#00BFFF] text-[#0A1F33] rounded-full w-7 h-7 flex items-center justify-center font-black shrink-0 mt-1">3</div>
                                <p class="text-slate-300"><span class="text-white font-bold">Precio Negociado:</span> Recibís el costo final real listo para cerrar.</p>
                            </div>
                        </div>
                        <a href="#contacto" class="inline-block bg-[#00BFFF] text-[#0A1F33] font-[900] py-4 px-10 rounded-xl uppercase tracking-widest shadow-2xl hover:scale-105 transition">Solicitar mi Reporte</a>
                    </div>
                    <div class="lg:w-1/2">
                        <div class="bg-white/5 p-2 rounded-[3rem] border border-white/10">
                            <img src="/service-shopper.webp" class="rounded-[2.8rem] opacity-90" alt="Metodología Datacar" />
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="py-24 bg-white">
            <div class="max-w-7xl mx-auto px-4">
                <div class="bg-[#0A1F33] rounded-[3.5rem] p-10 md:p-20 text-white flex flex-col md:flex-row items-center gap-12 shadow-2xl relative overflow-hidden">
                    <div class="md:w-3/5 relative z-10">
                        <h2 class="text-3xl md:text-5xl font-[900] text-[#00BFFF] mb-6 uppercase tracking-tight leading-none">¿Por qué nuestro servicio 0km es GRATIS?</h2>
                        <p class="text-slate-300 leading-relaxed text-lg font-light">Gracias a nuestros acuerdos corporativos con las representantes oficiales en Paraguay. Ellas nos pagan por acercarles compradores decididos. Vos te llevás nuestra **experiencia negociadora** y el ahorro de tiempo, sin pagar de más.</p>
                    </div>
                    <div class="md:w-2/5 border-l-0 md:border-l-2 border-white/10 md:pl-12 relative z-10">
                        <p class="font-bold text-xl italic leading-relaxed text-white">"Ahorramos US$ 1.500 en la compra de la camioneta de Jorge y gestionamos su transferencia en tiempo récord."</p>
                        <div class="mt-4 text-yellow-400 font-black tracking-widest uppercase text-[10px]">★★★★★ Caso Real Datacar</div>
                    </div>
                </div>
            </div>
        </section>

        <section id="comparador" class="py-24 bg-slate-100 border-y border-slate-200">
            <div class="max-w-7xl mx-auto px-4">
                <div class="text-center mb-16">
                    <h2 class="text-3xl md:text-5xl font-[900] text-[#0A1F33] mb-4 uppercase tracking-tighter">Veredicto Técnico</h2>
                    <p class="text-slate-500 text-lg font-light">Analizá las unidades finalistas de tu reporte ejecutivo aquí.</p>
                </div>

                <div id="loader" class="text-center py-20">
                    <div class="inline-block animate-spin rounded-full h-12 w-12 border-t-2 border-[#00BFFF]"></div>
                    <p class="mt-4 text-[#0A1F33] font-bold uppercase tracking-widest text-xs">Accediendo a la base de datos maestra...</p>
                </div>

                <div id="comparador-ui" class="hidden">
                    <div class="bg-white rounded-[2rem] p-8 border border-slate-200 shadow-sm mb-12">
                        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 items-end">
                            <div>
                                <label class="block text-[10px] font-black text-slate-400 mb-2 uppercase tracking-widest">Categoría</label>
                                <select id="filtroTipo" class="w-full bg-slate-50 border border-slate-200 text-[#0A1F33] text-sm rounded-xl p-4 font-bold outline-none"></select>
                            </div>
                            <div>
                                <label class="block text-[10px] font-black text-slate-400 mb-2 uppercase tracking-widest">Motor</label>
                                <select id="filtroCombustible" class="w-full bg-slate-50 border border-slate-200 text-[#0A1F33] text-sm rounded-xl p-4 font-bold outline-none">
                                    <option value="Todos">Todos</option>
                                    <option value="Tradicional">Combustión</option>
                                    <option value="Hibridos">Híbridos</option>
                                    <option value="Electrico">Eléctricos</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-[10px] font-black text-slate-400 mb-2 uppercase tracking-widest">Presupuesto</label>
                                <select id="filtroPrecio" class="w-full bg-slate-50 border border-slate-200 text-[#0A1F33] text-sm rounded-xl p-4 font-bold outline-none">
                                    <option value="Todos">Sin límite</option>
                                    <option value="25000">Hasta US$ 25.000</option>
                                    <option value="50000">Hasta US$ 50.000</option>
                                    <option value="80000">Hasta US$ 80.000</option>
                                </select>
                            </div>
                            <button id="btnLimpiar" class="text-[#00BFFF] font-bold underline text-sm pb-4">Reiniciar</button>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-10">
                        <div class="bg-white rounded-[2.5rem] shadow-xl border border-slate-100 flex flex-col overflow-hidden">
                            <div class="bg-slate-50 p-8 border-b">
                                <div class="space-y-4">
                                    <select id="marca1" class="w-full border p-4 rounded-xl font-bold bg-white outline-none"><option value="">Marca</option></select>
                                    <select id="modelo1" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Modelo</option></select>
                                    <select id="version1" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Versión</option></select>
                                </div>
                            </div>
                            <div id="resultado1" class="hidden p-8 flex-grow">
                                <div class="h-48 w-full flex items-center justify-center mb-8"><img id="img1" src="" class="max-h-full max-w-full object-contain mix-blend-multiply" /></div>
                                <h4 id="nombre1" class="text-2xl font-black text-slate-900 mb-8 leading-tight"></h4>
                                <div id="specs1" class="spec-container"></div>
                                <a id="cta1" href="#" target="_blank" class="block w-full bg-[#0A1F33] text-white text-center py-5 rounded-2xl mt-10 font-black uppercase text-xs tracking-widest shadow-lg">Negociar este Auto</a>
                            </div>
                        </div>
                        <div class="bg-white rounded-[2.5rem] shadow-xl border border-slate-100 flex flex-col overflow-hidden">
                            <div class="bg-slate-50 p-8 border-b">
                                <div class="space-y-4">
                                    <select id="marca2" class="w-full border p-4 rounded-xl font-bold bg-white outline-none"><option value="">Marca</option></select>
                                    <select id="modelo2" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Modelo</option></select>
                                    <select id="version2" disabled class="w-full border p-4 rounded-xl bg-white outline-none"><option value="">Versión</option></select>
                                </div>
                            </div>
                            <div id="resultado2" class="hidden p-8 flex-grow">
                                <div class="h-48 w-full flex items-center justify-center mb-8"><img id="img2" src="" class="max-h-full max-w-full object-contain mix-blend-multiply" /></div>
                                <h4 id="nombre2" class="text-2xl font-black text-slate-900 mb-8 leading-tight"></h4>
                                <div id="specs2" class="spec-container"></div>
                                <a id="cta2" href="#" target="_blank" class="block w-full bg-[#0A1F33] text-white text-center py-5 rounded-2xl mt-10 font-black uppercase text-xs tracking-widest shadow-lg">Negociar este Auto</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section id="contacto" class="py-24 bg-[#0A1F33]">
            <div class="max-w-xl mx-auto px-4">
                <div class="bg-white rounded-[3.5rem] p-10 md:p-14 shadow-2xl text-center border-t-8 border-[#00BFFF]">
                    <h3 class="text-2xl md:text-3xl font-black mb-2 text-[#0A1F33] uppercase">Activar mi Búsqueda</h3>
                    <p class="text-slate-400 mb-8 font-light text-sm uppercase tracking-widest">Reciba su reporte técnico en menos de 24hs.</p>
                    <form id="lead-form" class="space-y-5 text-left">
                        <input type="text" name="nombre" required placeholder="Nombre Completo" class="w-full border-b-2 border-slate-100 p-4 outline-none focus:border-[#00BFFF] text-lg transition-all">
                        <input type="tel" name="whatsapp" required placeholder="WhatsApp" class="w-full border-b-2 border-slate-100 p-4 outline-none focus:border-[#00BFFF] text-lg transition-all">
                        <select name="interes" class="w-full border-b-2 border-slate-100 p-4 outline-none text-gray-500 bg-transparent">
                            <option value="0km">Compra de un 0km</option>
                            <option value="usado">Compra de un Usado Seguro</option>
                            <option value="vender">Venta de mi auto actual</option>
                        </select>
                        <textarea name="mensaje" placeholder="Presupuesto aproximado o modelos..." class="w-full border-b-2 border-slate-100 p-4 outline-none focus:border-[#00BFFF] min-h-[100px] transition-all"></textarea>
                        <button type="submit" id="submit-btn" class="w-full bg-[#00BFFF] text-[#0A1F33] font-[900] py-6 rounded-2xl uppercase tracking-widest shadow-xl hover:scale-[1.02] transition-all">Activar mi Búsqueda</button>
                    </form>
                </div>
            </div>
        </section>

        <footer class="bg-[#05111D] text-slate-600 py-12 text-center">
            <p class="text-[10px] uppercase tracking-[0.4em]">© 2026 DATACAR EAS. Compra y venta de autos 0km y usados en Paraguay.</p>
        </footer>

        <script type="module" is:inline>
            import Papa from 'https://cdn.jsdelivr.net/npm/papaparse@5.4.1/+esm';

            const URL_CSV = "https://docs.google.com/spreadsheets/d/e/2PACX-1vQgA9AGiaH-RdEQrO2mOSjNDe7_zPoBMZyp-Yzmc0IKkTo9CluAzhWRJ9JXT-4XgNAAgiF-Uzzsea5m/pub?output=csv";
            const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbyTBqtK9wDzl10vc226zFwn4f-v-DVjKCfo9JVj3XCcepIcw41NQHchLrbDK6mz1zWgWg/exec';

            let bdOriginal = [];
            let bdFiltrada = [];
            let colMap = {};

            // Formulario
            document.getElementById('lead-form')?.addEventListener('submit', async (e) => {
                e.preventDefault();
                const btn = document.getElementById('submit-btn');
                btn.disabled = true; btn.innerText = "ACTIVANDO...";
                try {
                    await fetch(SCRIPT_URL, { method: 'POST', body: new FormData(e.target) });
                    window.location.href = '/gracias';
                } catch {
                    alert("Error de conexión. Intentá por WhatsApp.");
                    btn.disabled = false; btn.innerText = "Activar mi Búsqueda";
                }
            });

            // Motor de Comparación
            async function init() {
                try {
                    const res = await fetch(URL_CSV, { cache: "no-store" });
                    const text = await res.text();
                    Papa.parse(text, {
                        header: true, skipEmptyLines: true, transformHeader: h => h.trim(),
                        complete: (results) => {
                            bdOriginal = results.data.filter(r => r.Marca && r.Marca.trim() !== "");
                            const fields = results.meta.fields;
                            const f = (kws) => fields.find(f => kws.some(k => f.toLowerCase().includes(k.toLowerCase()))) || "";
                            
                            colMap = {
                                cat: fields[26],
                                precio: f(['precio']),
                                motor: f(['motor (cilindrada']),
                                trans: f(['transmisión']),
                                largo: f(['largo (mm)']),
                                baul: f(['baulera']),
                                plazas: f(['plazas']),
                                adas: f(['adas', 'seguridad']),
                                camara: f(['cámara']),
                                pantalla: f(['pantalla']),
                                conect: f(['conectividad']),
                                garantia: f(['garantía'])
                            };

                            poblarCategorias();
                            asignarEventos();
                            aplicarFiltros();
                            document.getElementById('loader').classList.add('hidden');
                            document.getElementById('comparador-ui').classList.remove('hidden');
                        }
                    });
                } catch(e) { console.error(e); }
            }

            function poblarCategorias() {
                const tipos = [...new Set(bdOriginal.map(c => c[colMap.cat]?.trim()).filter(Boolean))].sort();
                const select = document.getElementById('filtroTipo');
                const grupos = { 'SUV': [], 'Pick-up': [], 'Sedán': [], 'Hatchback': [], 'Otros': [] };
                tipos.forEach(t => {
                    const tl = t.toLowerCase();
                    if(tl.includes('suv')) grupos.SUV.push(t);
                    else if(tl.includes('pick')) grupos['Pick-up'].push(t);
                    else if(tl.includes('sed')) grupos.Sedán.push(t);
                    else if(tl.includes('hatch')) grupos.Hatchback.push(t);
                    else grupos.Otros.push(t);
                });
                select.innerHTML = '<option value="Todos">Todas las categorías</option>';
                for(const [g, items] of Object.entries(grupos)) {
                    if(items.length) {
                        const og = document.createElement('optgroup'); og.label = g;
                        items.forEach(i => og.appendChild(new Option(i, i)));
                        select.appendChild(og);
                    }
                }
            }

            function asignarEventos() {
                ['filtroTipo', 'filtroCombustible', 'filtroPrecio'].forEach(id => {
                    document.getElementById(id).addEventListener('change', aplicarFiltros);
                });
                document.getElementById('btnLimpiar').onclick = () => {
                    document.getElementById('filtroTipo').value = 'Todos';
                    document.getElementById('filtroCombustible').value = 'Todos';
                    document.getElementById('filtroPrecio').value = 'Todos';
                    aplicarFiltros();
                };
                [1, 2].forEach(col => {
                    document.getElementById(`marca${col}`).onchange = () => updateModelos(col);
                    document.getElementById(`modelo${col}`).onchange = () => updateVersiones(col);
                    document.getElementById(`version${col}`).onchange = () => mostrarAuto(col);
                });
            }

            function aplicarFiltros() {
                const fT = document.getElementById('filtroTipo').value;
                const fC = document.getElementById('filtroCombustible').value;
                const fP = document.getElementById('filtroPrecio').value;

                bdFiltrada = bdOriginal.filter(c => {
                    const pT = fT === 'Todos' || c[colMap.cat]?.trim() === fT;
                    const vP = parseInt(c[colMap.precio]?.replace(/\D/g, '')) || 0;
                    const pP = fP === 'Todos' || vP <= parseInt(fP);
                    const vL = (c.Combustible || "").toLowerCase();
                    const cat = vL.includes('hibrid') || vL.includes('hev') ? 'Hibridos' : (vL.includes('elect') || vL.includes('ev') ? 'Electrico' : 'Tradicional');
                    const pC = fC === 'Todos' || cat === fC;
                    return pT && pP && pC;
                });

                const arbol = {};
                bdFiltrada.forEach(c => {
                    const m = c.Marca.trim(), mo = (c.Modelo||"").trim(), v = (c.Versión||"Única").trim();
                    if(!arbol[m]) arbol[m] = {};
                    if(mo && !arbol[m][mo]) arbol[m][mo] = [];
                    if(mo && !arbol[m][mo].includes(v)) arbol[m][mo].push(v);
                });

                [1, 2].forEach(n => {
                    const s = document.getElementById(`marca${n}`);
                    s.innerHTML = '<option value="">Marca</option>';
                    Object.keys(arbol).sort().forEach(m => s.add(new Option(m, m)));
                    document.getElementById(`modelo${n}`).innerHTML = '<option value="">Modelo</option>';
                    document.getElementById(`modelo${n}`).disabled = true;
                    document.getElementById(`version${n}`).innerHTML = '<option value="">Versión</option>';
                    document.getElementById(`version${n}`).disabled = true;
                    document.getElementById(`resultado${n}`).classList.add('hidden');
                });
            }

            function updateModelos(n) {
                const m = document.getElementById(`marca${n}`).value;
                const s = document.getElementById(`modelo${n}`);
                s.innerHTML = '<option value="">Modelo</option>';
                if(m) {
                    const mods = [...new Set(bdFiltrada.filter(c => c.Marca.trim() === m).map(c => (c.Modelo||"").trim()))].sort();
                    mods.forEach(mo => s.add(new Option(mo, mo)));
                    s.disabled = false;
                } else s.disabled = true;
            }

            function updateVersiones(n) {
                const m = document.getElementById(`marca${n}`).value;
                const mo = document.getElementById(`modelo${n}`).value;
                const s = document.getElementById(`version${n}`);
                s.innerHTML = '<option value="">Versión</option>';
                if(mo) {
                    const vers = [...new Set(bdFiltrada.filter(c => c.Marca.trim() === m && (c.Modelo||"").trim() === mo).map(c => (c.Versión || "Única").trim()))].sort();
                    vers.forEach(v => s.add(new Option(v, v)));
                    s.disabled = false;
                } else s.disabled = true;
            }

            function mostrarAuto(n) {
                const m = document.getElementById(`marca${n}`).value;
                const mo = document.getElementById(`modelo${n}`).value;
                const v = document.getElementById(`version${n}`).value;
                const car = bdFiltrada.find(c => c.Marca.trim() === m && (c.Modelo||"").trim() === mo && (c.Versión || "Única").trim() === v);
                
                if(car) {
                    const specs = document.getElementById(`specs${n}`);
                    document.getElementById(`nombre${n}`).innerText = `${m} ${mo} ${v !== 'Única' ? v : ''}`;
                    document.getElementById(`img${n}`).src = car['URL de Imagen'] || '';
                    const adas = car[colMap.adas]?.trim();

                    specs.innerHTML = `
                        <div class="spec-card-row highlight-price-card"><span class="spec-label">Precio Final Negociado</span><span class="spec-value">${car[colMap.precio] || 'Consultar'}</span></div>
                        <div class="spec-card-row"><span class="spec-label">Mecánica & Transmisión</span><span class="spec-value">${car.Combustible} | ${car[colMap.motor]} | ${car[colMap.trans]}</span></div>
                        <div class="spec-card-row"><span class="spec-label">Dimensiones & Espacio</span><span class="spec-value">Largo: ${car[colMap.largo]}mm | Baúl: ${car[colMap.baul]}L | Plazas: ${car[colMap.plazas]}</span></div>
                        <div class="spec-card-row"><span class="spec-label">Seguridad Avanzada</span><span class="spec-value ${!adas ? 'text-red-500 font-bold' : ''}">${adas || 'Consultar ADAS con asesor'} | Cámaras: ${car[colMap.camara] || '-'}</span></div>
                        <div class="spec-card-row"><span class="spec-label">Tecnología</span><span class="spec-value">${car[colMap.pantalla] || '-'} | ${car[colMap.conect] || '-'}</span></div>
                        <div class="spec-card-row" style="border:none; background:transparent;"><span class="spec-label">Garantía Oficial</span><span class="spec-data-value text-slate-400 font-medium">${car[colMap.garantia] || 'Consultar'}</span></div>
                    `;

                    document.getElementById(`cta${n}`).href = `https://wa.me/595991244469?text=${encodeURIComponent('Hola Datacar, me interesa negociar el ' + m + ' ' + mo + ' ' + v)}`;
                    document.getElementById(`resultado${n}`).classList.remove('hidden');
                }
            }
            init();
        </script>
	</body>
</html>
